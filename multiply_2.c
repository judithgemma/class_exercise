#include <stdio.h>

int main() {
    int num, result;
    
    // Prompt user for input
    printf("Enter a number: ");
    scanf("%d", &num);
    
    // Multiply the number by 2
    result = num * 2;
    
    // Display the result
    printf("The result is: %d\n", result);
    
    return 0;
}
